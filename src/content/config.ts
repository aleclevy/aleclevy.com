import { defineCollection, z } from 'astro:content';

// Define your collections here if needed
const collections = {
  // Example: blog: defineCollection({ schema: z.object({}) })
};

export const config = { collections }; 